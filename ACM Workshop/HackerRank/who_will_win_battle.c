//done
//Golu r Molu te je pech re bhai
#include <stdio.h>

int main()
{
int x, y;
scanf("%d %d", &x,&y);
if (y>x)
{
    printf("Molu will get the treat");
} else if (y<x)
{
    printf("Golu will get the treat");
} else if (x==y)
{
    printf("Problem Solver will get the treat");
}
   return 0;
}
